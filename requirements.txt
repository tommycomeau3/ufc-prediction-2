# Tells python which libraries to install and minimum version required

# pandas is a library for data manipulation and analysis
pandas>=2.0.0
# numpy is a library for numerical computing
numpy>=1.24.0

# scikit-learn is a library for machine learning
scikit-learn>=1.3.0

# requests is a library for making HTTP requests
requests>=2.31.0
# beautifulsoup4 is a library for parsing HTML and XML
beautifulsoup4>=4.12.0
# lxml is a library for parsing XML and HTML
lxml>=4.9.0

# matplotlib is a library for data visualization
matplotlib>=3.7.0
# seaborn is a library for data visualization
seaborn>=0.12.0

# pyyaml is a library for parsing YAML files
pyyaml>=6.0

# Advanced ML models (scikit-learn is not used for advanced models)
xgboost>=2.0.0
lightgbm>=4.0.0

# Jupyter notebook support (for exploration)
jupyter>=1.0.0
# ipykernel is a library for Jupyter kernels
ipykernel>=6.25.0

# tqdm is a library for displaying progress bars
tqdm>=4.65.0
# python-dateutil is a library for parsing dates and times
python-dateutil>=2.8.0

# Flask is a library for creating web applications
flask>=3.0.0

